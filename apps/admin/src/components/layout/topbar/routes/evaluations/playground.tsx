import { type ActionGroup } from '@/components/layout/topbar/types'

// The playground now has an in-page command header so mode, prompt, run, and
// experiment actions stay attached to the workbench they affect.
export const EvaluationsPlaygroundActions: ActionGroup[] = []
