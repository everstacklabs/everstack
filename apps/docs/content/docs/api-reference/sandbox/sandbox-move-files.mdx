---
title: "POST Move / Rename File"
description: "Move or rename a file in a sandbox."
full: true
---

<APILayout>
<APIHeader>
<div className="mb-6">
  <h1 className="mb-2 text-2xl font-bold">Move / Rename File</h1>
  <div className="flex items-center gap-2">
    <span className="rounded-md px-2 py-0.5 text-xs font-bold uppercase bg-blue-500/10 text-blue-600 dark:text-blue-400">POST</span>
    <code className="text-sm text-fd-muted-foreground">{"/v1/sandbox/{sandbox_id}/files/mv"}</code>
  </div>
</div>
</APIHeader>
<APITryIt>
<APIPlayground method="post" path="/v1/sandbox/{'{sandbox_id}'}/files/mv" parameters={[{"name":"sandbox_id","in":"path","required":true}]} defaultBody={`{"source":"/repo/old.txt","destination":"/repo/new.txt"}`} />
</APITryIt>
<APIRequest>
<p className="mb-2 text-xs font-semibold uppercase tracking-wide text-fd-muted-foreground">Request</p>
<Tabs items={["cURL", "JavaScript", "Python"]}>
<Tab value="cURL">
```bash
curl -X POST "https://your-instance.example.com/v1/sandbox/sb_abc123/files/mv" \
  -H "Authorization: Bearer <token>" \
  -H "Content-Type: application/json" \
  -d '{
    "source": "/repo/old.txt",
    "destination": "/repo/new.txt"
  }'
```
</Tab>
<Tab value="JavaScript">
```js
const response = await fetch(
  "https://your-instance.example.com/v1/sandbox/sb_abc123/files/mv",
  {
    method: "POST",
    headers: {
      Authorization: "Bearer <token>",
      "Content-Type": "application/json",
    },
    body: JSON.stringify({
      source: "/repo/old.txt",
      destination: "/repo/new.txt",
    }),
  }
);

const data = await response.json();
console.log(data); // { status: "moved" }
```
</Tab>
<Tab value="Python">
```python
import requests

response = requests.post(
    "https://your-instance.example.com/v1/sandbox/sb_abc123/files/mv",
    headers={"Authorization": "Bearer <token>"},
    json={
        "source": "/repo/old.txt",
        "destination": "/repo/new.txt",
    },
)

print(response.json())  # {"status": "moved"}
```
</Tab>
</Tabs>
</APIRequest>
<APIResponse>
<p className="mb-2 text-xs font-semibold uppercase tracking-wide text-fd-muted-foreground">Response</p>
<Tabs items={["200", "default"]}>
<Tab value="200">
```json
{"status": "moved"}
```
</Tab>
<Tab value="default">
```json
{"code": 0, "message": "string", "details": [{"@type": "string"}]}
```
</Tab>
</Tabs>
</APIResponse>
<APIContent>

### Description
Moves or renames a file within the sandbox filesystem. Provide the current absolute path as `source` and the desired absolute path as `destination`. If `source` and `destination` share the same parent directory, the operation is equivalent to a rename. Moving a file across directories is also supported.

### Path Parameters
| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `sandbox_id` | `string` | Yes | Sandbox ID or name |

### Request Body
| Property | Type | Required | Description |
|----------|------|----------|-------------|
| `source` | `string` | Yes | Absolute path of the file to move or rename |
| `destination` | `string` | Yes | Absolute path of the desired new location or name |

### Responses
#### 200 Success
| Property | Type | Description |
|----------|------|-------------|
| `status` | `string` | Always `"moved"` on success |

#### default An unexpected error response.
| Property | Type | Description |
|----------|------|-------------|
| `code` | `integer` | |
| `message` | `string` | |
| `details` | `object[]` | |

</APIContent>
</APILayout>
